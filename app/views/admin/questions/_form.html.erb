<h1><%= question_header(@question) %></h1>
<%= form_with(model: [@test, @question], local: true) do |form| %>
  <%= form.label :body %>
  <%= form.text_field :body %>
  <p><%= form.submit %></p>
<% end %>

<%= link_to 'Back', @question.tests %>
